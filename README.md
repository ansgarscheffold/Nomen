# Nomen
Nomen is a native macOS app that suggests archive-style file names for text and PDF documents using on-device AI. It reads supported files, extracts text (including OCR for scanned PDFs), proposes a structured title and date, and lets you preview and apply renames in bulk.
